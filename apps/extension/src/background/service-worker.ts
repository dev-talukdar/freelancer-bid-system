import { toFreelancerProjectUrl } from '@fbs/shared'; import { LocalApiClient } from '../services/local-api.js'; import { addNotifiedId, getSettings, hasNotified } from '../storage/settings.js'; import { createProjectNotification } from './notifications.js';
chrome.runtime.onInstalled.addListener(()=>{void chrome.alarms.create('pollBackend',{periodInMinutes:0.5});});
async function ensureOffscreen(){ if (await chrome.offscreen.hasDocument()) return; await chrome.offscreen.createDocument({url:'offscreen.html',reasons:['AUDIO_PLAYBACK'],justification:'Play local alert sound for new Freelancer project notifications'}); }
async function playSound(){ await ensureOffscreen(); chrome.runtime.sendMessage({type:'PLAY_ALERT_SOUND'}); }
async function check(){ const settings=await getSettings(); if(!settings.localApiSecret) return; const api=new LocalApiClient(settings); const projects=await api.detected(true,10); for(const p of projects.items){ if(await hasNotified(p.freelancerProjectId)) continue; await createProjectNotification(p); await addNotifiedId(p.freelancerProjectId); await api.markRead(p.id); await playSound(); }}
chrome.alarms.onAlarm.addListener(alarm=>{ if(alarm.name==='pollBackend') void check(); });
chrome.notifications.onClicked.addListener(id=>{ const projectId=id.replace('project:',''); void (async()=>{ const settings=await getSettings(); const api=new LocalApiClient(settings); const found=(await api.detected(false,50)).items.find(p=>p.id===projectId); if(found){ await chrome.tabs.create({url:toFreelancerProjectUrl(found.seoUrl)}); await api.opened(found.id); } })(); });
