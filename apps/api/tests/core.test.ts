import { describe, expect, it } from 'vitest'; import { normalizeFreelancerSeoUrl } from '@fbs/shared'; import { buildFreelancerQuery } from '../src/app/modules/freelancer-client/query.js'; import { parseRateLimitLimit, calculateAdaptiveDelay } from '../src/app/modules/freelancer-client/rate-limit.js'; import { projectMatches } from '../src/app/modules/project-monitor/filter.js'; import { PollLock } from '../src/app/modules/project-monitor/lock.js'; import { mapFreelancerError } from '../src/app/error/app-error.js';
const profile={keywords:['node'],excludedKeywords:['spam'],jobIds:[9],countries:['us'],languages:['en'],projectTypes:['fixed'],allowLocalProjects:false,maximumBidCount:5} as never;
const project={id:1,title:'Node API',type:'fixed',status:'active',seo_url:'node-api',language:'en',local:false,bid_stats:{bid_count:2},jobs:[{id:9,name:'Node'}],location:{country:{code:'us'}}};
describe('api foundations',()=>{ it('serializes repeated array query parameters',()=>{ const qs=buildFreelancerQuery({jobs:[9,500],countries:['us','ca'],compact:true}); expect(qs.toString()).toContain('jobs%5B%5D=9'); expect(qs.getAll('jobs[]')).toEqual(['9','500']); }); it('parses rate limit headers and adapts delay',()=>{ expect(parseRateLimitLimit('50, 1000;window=3600')[1]?.windowSeconds).toBe(3600); expect(calculateAdaptiveDelay(30000,{windows:[{limit:50}],remaining:5})).toBe(60000); }); it('filters local projects and matches configured filters',()=>{ expect(projectMatches(profile,project)).toBe(true); expect(projectMatches(profile,{...project,local:true})).toBe(false); }); it('normalizes SEO URLs safely',()=>{ expect(normalizeFreelancerSeoUrl('/foo/bar')).toBe('foo/bar'); expect(()=>normalizeFreelancerSeoUrl('../bad')).toThrow(); }); it('locks concurrent polls',async()=>{ const lock=new PollLock(); await expect(Promise.all([lock.runExclusive(()=>new Promise(r=>setTimeout(()=>r(true),20))),lock.runExclusive(async()=>true)])).rejects.toThrow('Poll already running'); }); it('maps freelancer api errors',()=>{ const err=mapFreelancerError(429,{message:'slow down'},'rid'); expect(err.errorCode).toBe('FREELANCER_RATE_LIMITED'); expect(err.freelancerRequestId).toBe('rid'); }); });
